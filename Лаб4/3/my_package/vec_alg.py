def vec_sum(A,B):
    return [A[0] + B[0], A[1] + B[1]]

def vec_sub(A,B):
    return [A[0] - B[0], A[1] - B[1]]

def vec_mul(A,B):
    return [A[0] * B[0], A[1] * B[1]]

def vec_mul_n(A,a):
    return [A[0] * a, A[1] * a]
