def greet(name: str) -> None:
    print(f"Greetings, {name}!")

def square(number: float | int) -> float:
    return number * number

def max_of_two(x: float|int, y: float|int) -> float:
    return max(x,y)
